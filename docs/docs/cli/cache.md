```text
usage: pum cache [-h] [--force] {path,list,clear}
```

### positional arguments:

- `{path,list,clear}`: Action to perform: path (print the cache directory), list (show the cached prefixes), clear (delete them)

### options:

- `-h, --help`: show this help message and exit
- `--force`: Skip confirmation prompt for clear action
